import '../app_exports.dart';
class RegistrationScreen extends StatelessWidget {
  // Add the necessary properties and methods for the registration screen
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Registration'),
      ),
      // Your registration UI goes here
    );
  }
}
